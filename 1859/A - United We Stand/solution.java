import java.util.*; public class Main {        public static void main(String[] args) {            Scanner sc=new Scanner(System.in);        int t=sc.nextInt();                 while (t-->0) {            int n=sc.nextInt();            int[] a=new int[n];                        for (int i=0; i<n; i++) {                a[i]=sc.nextInt();            }                        int maxElement = Arrays.stream(a).max().getAsInt();                        ArrayList<Integer> b = new ArrayList<>();            ArrayList<Integer> c = new ArrayList<>();                        for (int num : a) {                if (num==maxElement) {                    c.add(num);                } else {                    b.add(num);                }            }                        if (b.isEmpty() || c.isEmpty()) {                System.out.println("-1");            } else {                System.out.println(b.size()+" "+c.size());                printArray(b);                printArray(c);            }        }    }        private static void printArray(ArrayList<Integer> list) {        for (int num:list) {            System.out.print(num+" ");        }        System.out.println();    }}